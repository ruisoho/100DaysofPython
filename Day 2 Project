#Ask for how much is the bill
bill = input("Hello, how much is your bill?\n")

#Ask how many persons will split the bill
split = input("How many persons will split the bill?\n")

#Ask how much a percent you would like to tip
tip = input("How much percent would you like to tip? 10%, 15% or 20%?\n")

#Calculate the tip
tip_percent = int(tip) / 100
#Calculate the total bill
total_bill = float(bill) * (1 + tip_percent) / float(split)

print(f"The amount that each of you need to pay is ${round(total_bill ,2)}. Thank you for using the tip calculator!")

